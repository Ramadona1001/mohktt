[pytest]
DJANGO_SETTINGS_MODULE = mukhattat.settings
python_files = tests.py test_*.py *_tests.py
python_classes = Test*
python_functions = test_*
addopts = 
    --nomigrations
    --reuse-db
    --cov=.
    --cov-report=html
    --cov-report=term-missing
    --cov-report=xml
    -v
testpaths = .

