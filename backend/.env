# Django Settings
SECRET_KEY=django-insecure-1234567890abcdefghijklmnopqrstuvwxyz
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# Database Configuration
# Set USE_POSTGRES=True to use PostgreSQL, False for SQLite (default)
USE_POSTGRES=False

# PostgreSQL Configuration (only used if USE_POSTGRES=True)
DB_NAME=mukhattat_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# Redis Configuration (for Celery and Channels)
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0
REDIS_URL=redis://localhost:6379/1

# Email Configuration
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your_email@gmail.com
EMAIL_HOST_PASSWORD=your_password_here
DEFAULT_FROM_EMAIL=noreply@mukhattat.com

# Document Review Timer (in days)
DOCUMENT_REVIEW_TIMER_DAYS=10

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Time Zone and Language
TIME_ZONE=Africa/Cairo
LANGUAGE_CODE=ar

# Static and Media URLs
STATIC_URL=/static/
MEDIA_URL=/media/
